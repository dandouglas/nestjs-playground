/*
* Do not expose this key publicly.
* We have done so here for simplicity, but in a production system you must protect
* this key using appropriate measures such as a secrets vault, environment variable, or configuration service.
*/

export const jwtConstants = {
  secret: 'secretKey',
};
